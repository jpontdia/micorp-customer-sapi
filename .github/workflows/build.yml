name: Build and deploy to Anypoint Exchange
 
on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  Mulesoft:
    uses: jpontdia/mule-build-and-deploy/.github/workflows/mulesoft.yml@main
    with:
      keyvault-key: jpworks-keyvault
    secrets:
      azure-credentials: ${{ secrets.AZURE_CREDENTIALS }}
      configurations-token: ${{ secrets.CONFIGURATIONS_TOKEN }}

      